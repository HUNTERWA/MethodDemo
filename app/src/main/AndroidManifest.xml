<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rohit.methoddemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Aadhaar" />
        <activity android:name=".TermsAndCondition" />
        <activity android:name=".AadhaarLink" />
        <activity android:name=".NoAadhaar" />
        <activity android:name=".Declaration" />
        <activity android:name=".MyAadhaarDetails" />
        <activity android:name=".MyAadhaarInformation" />
        <activity android:name=".UserMode" />
        <activity android:name=".AdminMaster"></activity>
    </application>

</manifest>